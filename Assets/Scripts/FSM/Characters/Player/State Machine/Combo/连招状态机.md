### 流程图
┌──────────────┐     按下攻击键      ┌─────────────────┐
│              │ ──────────────────? │                 │
│  NullState   │                     │  ATKIngState    │
│  (空闲状态)   │ ?────────────────── │  (攻击中状态)    │
│              │   动画结束+无连招    │                 │
└──────┬───────┘                     └─────────────────┘
       │                                    ▲
       │ 按下Q/E键                          │ 连招衔接
       ▼                                    │
┌──────────────┐                           │
│              │ ──────────────────────────┘
│ SkillState   │   技能动画结束
│  (技能状态)   │
│              │
└──────────────┘

### 设计亮点
? 状态预实例化：避免运行时 GC 压力
? 数据分离：静态配置（ScriptableObject）与动态数据（ReusableData）解耦
? 事件驱动：输入系统与状态机通过事件解耦
? 可扩展性：新增状态只需继承 PlayerComboState 并注册到状态机
? 相机集成：技能状态自动管理镜头切换，提升战斗表现力


## ? ZZZDemo 连招系统详细流程图

### ? **一、系统整体架构**

```mermaid
graph TB
    subgraph "输入层"
        A[玩家输入] --> B[CharacterInputSystem]
        B --> C{输入类型判断}
    end
    
    subgraph "状态管理层"
        C -->|左键攻击| D[PlayerComboStateMachine]
        C -->|Q/E技能| D
        D --> E{当前状态}
        E -->|空闲| F[NullState]
        E -->|攻击中| G[ATKIngState]
        E -->|技能中| H[SkillState]
    end
    
    subgraph "连招执行层"
        F --> I[CharacterCombo]
        G --> I
        H --> I
        I --> J[连招逻辑处理]
        J --> K[动画播放]
        J --> L[伤害判定]
        J --> M[特效/音效]
    end
    
    subgraph "数据层"
        N[ComboContainerData<br/>连招容器] --> I
        O[ComboData<br/>单段招式数据] --> N
        P[PlayerComboReusableData<br/>运行时数据] --> I
    end
    
    subgraph "表现层"
        K --> Q[Animator]
        L --> R[GameEventManager]
        M --> S[SFX/VFX PoolManager]
        K --> T[CameraSwitcher]
        L --> U[CameraHitFeel<br/>震屏/顿帧]
    end
```

---

### ? **二、核心状态流转图**

```mermaid
stateDiagram-v2
    [*] --> NullState: 初始化
    
    NullState --> ATKIngState: 按下左键<br/>CanBaseComboInput=true
    NullState --> SkillState: 按下Q/E键<br/>CanSkillInput=true
    
    ATKIngState --> ATKIngState: 连招衔接<br/>预输入窗口期内再次按左键
    ATKIngState --> NullState: 动画结束<br/>无连招输入
    ATKIngState --> SkillState: 按下Q/E键<br/>打断普通攻击
    
    SkillState --> NullState: 技能动画结束
    
    note right of NullState
        - 重置连招索引
        - 等待用户输入
        - 可接收任何攻击指令
    end note
    
    note right of ATKIngState
        - 播放攻击动画
        - 检测敌人朝向
        - 处理移动打断
        - 管理预输入缓冲
    end note
    
    note right of SkillState
        - 锁定移动状态
        - 切换特写镜头
        - 播放技能动画
        - 禁用其他输入
    end note
```

---

### ?? **三、普通攻击连招流程（核心）**

```mermaid
flowchart TD
    Start([玩家按下左键]) --> CheckInput{CanBaseComboInput?}
    
    CheckInput -->|否| End1([忽略输入])
    CheckInput -->|是| CheckRun{是否在冲刺/闪避?}
    
    CheckRun -->|是| SetRunCombo[currentCombo = runCombo<br/>跑步攻击]
    CheckRun -->|否| CheckCurrent{currentCombo是否为空<br/>或不等于lightCombo?}
    
    CheckCurrent -->|是| SetLightCombo[currentCombo = lightCombo<br/>重置连招信息]
    CheckCurrent -->|否| KeepCombo[保持当前连招容器]
    
    SetRunCombo --> MarkCommand
    SetLightCombo --> MarkCommand
    KeepCombo --> MarkCommand
    
    MarkCommand[标记有攻击指令<br/>hasATKCommand = true<br/>禁止新输入 canInput = false] --> WaitFrame[等待下一帧Update]
    
    WaitFrame --> CheckCanATK{canATK == true?}
    CheckCanATK -->|否| WaitFrame
    CheckCanATK -->|是| CheckHasCommand{hasATKCommand == true?}
    CheckHasCommand -->|否| WaitFrame
    CheckHasCommand -->|是| GetComboName[获取当前段动画名<br/>comboName = currentCombo.GetComboName<br/>comboIndex]
    
    GetComboName --> PlayAnim[animator.CrossFadeInFixedTime<br/>播放动画]
    PlayAnim --> UpdateInfo[更新连招信息<br/>comboIndex++<br/>canConnect = true<br/>canMoveInterrupt = false]
    
    UpdateInfo --> ResetFlag[重置标志<br/>hasATKCommand = false<br/>canATK = false]
    
    ResetFlag --> AnimEventWait{等待动画事件}
    
    AnimEventWait -->|ATK事件| DamageCheck[伤害判定 ATK]
    AnimEventWait -->|EnablePreInput事件| EnableInput[启用预输入<br/>canInput = true]
    AnimEventWait -->|CancelAttackColdTime事件| CancelCold[取消冷却<br/>canATK = true]
    AnimEventWait -->|DisableLinkCombo事件| DisableLink[断开连招<br/>canConnect = false]
    AnimEventWait -->|EnableMoveInterrupt事件| EnableMove[允许移动打断<br/>canMoveInterrupt = true]
    
    DamageCheck --> CheckEnemy{检测到敌人?}
    CheckEnemy -->|是| CalcDamage[计算伤害<br/>触发GameEventsManager<br/>播放震屏/顿帧]
    CheckEnemy -->|否| SkipDamage[跳过伤害]
    
    CalcDamage --> PlaySFX[播放音效<br/>SFX_PoolManager]
    PlayVFX[播放特效<br/>VFX_PoolManager] --> PlaySFX
    SkipDamage --> PlaySFX
    
    PlaySFX --> AnimExit{动画结束事件?<br/>OnAnimationExitEvent}
    
    AnimExit -->|是| DelayCheck[延迟0.2秒检查]
    AnimExit -->|否| WaitNextInput[等待下一帧输入]
    
    DelayCheck --> StillATK{仍在ATK标签?}
    StillATK -->|是| WaitNextInput
    StillATK -->|否| SwitchNull[切换到NullState]
    
    WaitNextInput --> CheckPreInput{预输入窗口期?<br/>canInput == true &&<br/>canConnect == true}
    
    CheckPreInput -->|是且按下左键| LoopBack[回到GetComboName<br/>连招衔接下一段]
    CheckPreInput -->|否或超时| CheckMoveInterrupt{canMoveInterrupt &&<br/>有移动输入?}
    
    CheckMoveInterrupt -->|是| InterruptMove[移动打断<br/>播放Locomotion动画<br/>切换到NullState]
    CheckMoveInterrupt -->|否| Timeout[连招超时<br/>切换到NullState]
    
    LoopBack --> CheckMaxIndex{comboIndex >= MaxCount?}
    CheckMaxIndex -->|是| ResetIndex[重置comboIndex = 0<br/>回到第一段]
    CheckMaxIndex -->|否| KeepIndex[保持当前索引]
    
    ResetIndex --> GetComboName
    KeepIndex --> GetComboName
    
    style Start fill:#90EE90
    style End1 fill:#FFB6C1
    style SwitchNull fill:#87CEEB
    style InterruptMove fill:#FFA500
    style Timeout fill:#FFA500
    style DamageCheck fill:#FFD700
```

---

### ? **四、技能释放流程**

```mermaid
flowchart TD
    SkillStart([玩家按下Q/E键]) --> CheckSkillInput{CanSkillInput?}
    
    CheckSkillInput -->|否| SkillEnd1([忽略输入])
    CheckSkillInput -->|是| CheckCharacter{当前角色匹配?}
    
    CheckCharacter -->|否| SkillEnd1
    CheckCharacter -->|是| DetermineSkillType{按键类型}
    
    DetermineSkillType -->|E键| LoadFinishSkill[currentSkill = finishSkillCombo]
    DetermineSkillType -->|Q键| LoadNormalSkill[currentSkill = skillCombo]
    
    LoadFinishSkill --> ChangeToSkillState
    LoadNormalSkill --> ChangeToSkillState
    
    ChangeToSkillState[PlayerComboStateMachine.ChangeState<br/>切换到SkillState] --> SkillEnter[SkillState.Enter]
    
    SkillEnter --> LockMovement[锁定移动状态机<br/>切换到playerMovementNullState]
    LockMovement --> SwitchCamera[激活技能专属相机<br/>CameraSwitcher.ActiveStateCamera]
    
    SwitchCamera --> PlaySkillAnim[播放技能动画<br/>animator.CrossFadeInFixedTime]
    
    PlaySkillAnim --> SkillUpdate[每帧Update<br/>更新攻击朝向敌人]
    
    SkillUpdate --> SkillAnimEvent{等待动画事件}
    
    SkillAnimEvent -->|ATK事件| SkillDamage[技能伤害判定]
    SkillAnimEvent -->|Exit事件| SkillExit[OnAnimationExitEvent]
    
    SkillDamage --> CheckSkillEnemy{检测到敌人?}
    CheckSkillEnemy -->|是| SkillHit[触发技能伤害事件<br/>播放特效音效]
    CheckSkillEnemy -->|否| SkillMiss[技能未命中]
    
    SkillHit --> SkillExit
    SkillMiss --> SkillExit
    
    SkillExit --> RestoreCamera[恢复默认相机<br/>UnActiveStateCamera]
    RestoreCamera --> SwitchToNull[切换到NullState]
    
    SwitchToNull --> SkillComplete([技能释放完成])
    
    style SkillStart fill:#90EE90
    style SkillEnd1 fill:#FFB6C1
    style SkillComplete fill:#90EE90
    style SkillDamage fill:#FFD700
    style ChangeToSkillState fill:#87CEEB
```

---

### ? **五、数据结构关系图**

```mermaid
classDiagram
    class PlayerComboStateMachine {
        +Player Player
        +PlayerComboReusableData ReusableData
        +PlayerATKIngState ATKIngState
        +PlayerNullState NullState
        +PlayerSkillState SkillState
        +ChangeState(IState)
        +HandInput()
        +Update()
    }
    
    class PlayerComboReusableData {
        +int currentIndex
        +bool canInput
        +bool canATK
        +bool hasATKCommand
        +bool canLink
        +bool canMoveInterrupt
        +ComboContainerData currentCombo
        +ComboData currentSkill
        +int comboIndex
        +int ATKIndex
    }
    
    class ComboContainerData {
        +List~ComboData~ comboDates
        +ComboData DodgeATKData
        +ComboData BackDodgeATKData
        +GetComboName(int)
        +GetComboDamage(int)
        +GetComboMaxCount()
        +GetComboSoundStyle(int)
    }
    
    class ComboData {
        +string comboName
        +float comboDamage
        +float comboColdTime
        +float attackDistance
        +AttackStyle attackStyle
        +AudioClip[] weaponSound
        +float[] shakeForceList
        +float[] pauseFrameTimeList
        +int ATKCount
    }
    
    class CharacterCombo {
        -Animator animator
        -PlayerComboReusableData reusableData
        -ComboContainerData currentCombo
        +LightComboInput()
        +SkillInput()
        +ATK()
        +UpdateComboAnimation()
        +CheckCanLinkCombo()
    }
    
    PlayerComboStateMachine --> PlayerComboReusableData : 持有
    PlayerComboStateMachine --> CharacterCombo : 通过状态访问
    CharacterCombo --> ComboContainerData : 使用
    ComboContainerData --> ComboData : 包含多个
    PlayerComboReusableData --> ComboContainerData : 引用当前
    PlayerComboReusableData --> ComboData : 引用当前技能
```

---

### ? **六、动画事件时序图**

```mermaid
sequenceDiagram
    participant Player as 玩家
    participant Input as CharacterInputSystem
    participant FSM as PlayerComboStateMachine
    participant State as CurrentState
    participant Combo as CharacterCombo
    participant Anim as Animator
    participant Event as GameEventManager
    participant Pool as SFX/VFX PoolManager
    
    Player->>Input: 按下左键
    Input->>FSM: HandInput()
    FSM->>State: HandInput()
    State->>Combo: LightComboInput()
    Combo->>Combo: hasATKCommand = true
    
    Note over Combo,Anim: 下一帧Update
    
    Combo->>Combo: UpdateComboAnimation()
    Combo->>Anim: CrossFadeInFixedTime(comboName)
    Combo->>Combo: comboIndex++, canConnect = true
    
    Note over Anim: 动画播放中...
    
    Anim->>State: OnAnimationTranslateEvent(ATK)
    State->>Combo: ATK()
    Combo->>Combo: AttackDetection()
    
    alt 检测到敌人
        Combo->>Event: CallEvent("造成伤害")
        Event->>Pool: 播放打击音效
        Event->>Pool: 播放打击特效
        Note over Event: 触发震屏/顿帧
    end
    
    Anim->>State: OnAnimationTranslateEvent(EnablePreInput)
    State->>Combo: CanInput()
    Combo->>Combo: canInput = true
    
    Note over Player,Combo: 预输入窗口期
    
    alt 玩家在窗口期内再次按下左键
        Player->>Input: 按下左键
        Input->>FSM: HandInput()
        FSM->>State: HandInput()
        State->>Combo: LightComboInput()
        Combo->>Combo: 连招衔接，comboIndex++
        Combo->>Anim: 播放下一段动画
    else 玩家未输入
        Anim->>State: OnAnimationExitEvent()
        State->>FSM: 延迟0.2秒后检查
        FSM->>FSM: 切换到NullState
        FSM->>Combo: ReSetComboInfo()
    end
```

---

### ? **七、关键机制说明**

#### **1. 预输入缓冲机制（Pre-Input Buffer）**
```
时间轴：|--攻击动画--|--预输入窗口--|--空闲--|
                    ↑              ↑
                 可以输入       超时则断连
```
- **实现方式**：通过 [canInput](file://e:\lzy\_Tools\unity_dev\zzzdemo\Assets\Scripts\FSM\Characters\Player\Data\States\PlayerComboReusableData.cs#L20-L20) 和 [canConnect](file://e:\lzy\_Tools\unity_dev\zzzdemo\Assets\Scripts\Combo\CharacterComboBase.cs#L36-L36) 两个标志控制
- **触发动画事件**：[EnablePreInput](file://e:\lzy\_Tools\unity_dev\zzzdemo\Assets\Scripts\FSM\Characters\Player\State%20Machine\Combo\States\ComboStates\PlayerATKIngState.cs#L25-L28) 开启输入，[DisableLinkCombo](file://e:\lzy\_Tools\unity_dev\zzzdemo\Assets\Scripts\FSM\Characters\Player\State%20Machine\Combo\States\ComboStates\PlayerATKIngState.cs#L33-L36) 关闭连招
- **目的**：让玩家在动画结束前按下攻击键也能流畅衔接，提升操作手感

#### **2. 移动打断机制（Move Interrupt）**
```
攻击动画播放 → EnableMoveInterrupt事件 → canMoveInterrupt = true
                                        ↓
                                  检测到移动输入
                                        ↓
                                  中断攻击动画
                                        ↓
                                  切换到移动状态
```
- **应用场景**：某些轻量攻击可通过移动取消后摇
- **控制变量**：[canMoveInterrupt](file://e:\lzy\_Tools\unity_dev\zzzdemo\Assets\Scripts\FSM\Characters\Player\Data\States\PlayerComboReusableData.cs#L28-L28)

#### **3. 连招断开机制（Combo Break）**
```
触发条件：
1. DisableLinkCombo 动画事件触发
2. 预输入窗口期超时
3. 玩家按下奔跑键
4. 移动打断触发
```

#### **4. 伤害判定机制**
```
ATK动画事件触发
    ↓
检测敌人（SphereCast射线检测）
    ↓
距离判断 + 角度判断
    ↓
触发GameEventManager事件
    ↓
通知Health系统扣血
    ↓
播放打击反馈（音效/特效/震屏/顿帧）
```

---

### ? **八、视觉反馈流程**

```mermaid
graph LR
    A[伤害判定成功] --> B[CameraHitFeel]
    B --> C[震屏 CameraShake]
    B --> D[顿帧 PauseFrame]
    B --> E[时缓 TimeSlow]
    
    A --> F[SFX_PoolManager]
    F --> G[武器音效]
    F --> H[角色语音]
    
    A --> I[VFX_PoolManager]
    I --> J[打击特效]
    I --> K[技能特效]
    
    A --> L[Animator]
    L --> M[受击动画]
    L --> N[攻击动画]
```

---

### ? **九、完整连招示例流程**

以「三段普通攻击」为例：

```
时间线     玩家操作          系统状态               动画事件
─────────────────────────────────────────────────────────────
T0        按下左键         NullState→ATKIngState  
                            设置currentCombo       
                            comboIndex=0           
                            
T0+1帧                     播放第1段攻击动画      
                            
T0+0.3s                    ATK事件触发            
                            伤害判定 → 播放音效    
                            
T0+0.5s                    EnablePreInput         
                            canInput=true          
                            
T0+0.6s   按下左键         检测到预输入           
                            comboIndex=1           
                            播放第2段攻击动画      
                            
T0+0.9s                    ATK事件触发            
                            第2段伤害判定          
                            
T0+1.0s                    EnablePreInput         
                            
T0+1.1s   按下左键         检测到预输入           
                            comboIndex=2           
                            播放第3段攻击动画      
                            
T0+1.5s                    DisableLinkCombo       
                            canConnect=false       
                            
T0+1.8s                    OnAnimationExitEvent   
                            延迟0.2秒检查          
                            
T0+2.0s                    切换到NullState        
                            ReSetComboInfo()       
                            comboIndex重置为0      
```

---

这份流程图涵盖了从**输入检测** → **状态管理** → **连招执行** → **伤害判定** → **视听反馈**的完整链路。如果你想深入了解某个具体环节（比如对象池的实现、伤害计算细节、或者如何扩展新的连招类型），我可以继续为你详细解析！